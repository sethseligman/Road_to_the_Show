# Local secrets — not committed (see web/.gitignore).
# Paste your Anthropic API key after the = sign (no quotes).

ANTHROPIC_API_KEY=

# Optional: Vercel KV (narrate cache)
# KV_REST_API_URL=
# KV_REST_API_TOKEN=
